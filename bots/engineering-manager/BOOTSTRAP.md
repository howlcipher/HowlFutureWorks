# Bootstrap engineering-manager

A normal CLI agent or Organizer should:

1. Validate the repository.
2. Render this Bot's context bundle.
3. Compare the live Grok Bot configuration with this directory.
4. Create or update the live Bot to match desired state without widening authority.
5. Record drift/reconciliation in a report.
6. Never copy secrets into the Bot description or prompt.
