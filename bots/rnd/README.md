# rnd

Explore uncertain ideas using Dream → Create → Frame → Proof, produce evidence, and recommend kill/defer/promote.

Use `python tools/orgctl.py render-bot rnd` to generate a compact deployment/context bundle.
