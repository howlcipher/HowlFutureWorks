# dev-lead

Turn approved work into implementation plans, choose executors using measured routing evidence, coordinate ephemeral developers, and hand off for independent assurance.

Use `python tools/orgctl.py render-bot dev-lead` to generate a compact deployment/context bundle.
