# Security review — HOWL-002 contribution auto-id

- Reviewer: `bot-assurance-0001`
- When: 2026-09-20T22:09:00-04:00
- Tip: `b7e55fc6c6d1ef751b825c3e26a57c344149e833`
- Related: `reports/audits/assurance-verification-HOWL-002-2026-09-20.md`

## Threats considered

1. **Control bypass via identifier vocabulary** — generating ids that fail `safe_id` could train operators to supply bespoke ids or pressure weakening of `safe_id`.
2. **Scope creep / latent authority change** — a broad orgctl refactor could alter checkpoint, hire, or separation paths.
3. **Evidence integrity** — verification must not rely solely on implementer claims.

## Observations

- Diff vs `origin/main` is three paths; product code change is one `strftime` format character in `record_contribution`.
- Checkpoint continues to use lowercase `t`; separation proposal stamp still uppercase `T` (out of HOWL-002 scope).
- Independent CLI and pytest runs reproduced fixed auto path and preserved reject path for invalid explicit ids.
- `safe_id` regex unchanged — generator was corrected, not the gate weakened.

## Security disposition

**PASS** for HOWL-002. Residual note: track uppercase-`T` separation stamps as a separate integrity hygiene item if those values ever enter `safe_id`-gated APIs.
