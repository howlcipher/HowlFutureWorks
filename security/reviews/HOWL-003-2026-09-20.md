# Security Review — HOWL-003 (2026-09-20)

**PR:** https://github.com/howlcipher/HowlFutureWorks/pull/2  
**Tip:** `fd04b190ffd5c44af5401e3a945cbfff837d2d6e`  
**Reviewer role:** Assurance (security section)  
**Verdict:** **PASS**

## Change summary
`tools/package_release.py` adds `packaging_main_ref(commit)` so `git bundle create` can name `main` on detached checkouts without a local main. Tests cover local-main, detached/no-main, and divergent-main restore. Work-item metadata added under `reports/work-items/HOWL-003/`.

## Security analysis
| Topic | Finding |
|-------|---------|
| Ref rewrite / leave-behind | finally restores previous main SHA or deletes ref if absent; pytest asserts both paths |
| Privilege / supply chain | No new network deps; bundle still built from local HEAD only |
| Secrets in delta | Pattern scan CLEAN on all AM files vs main |
| CI weakening | `.github/workflows/validate.yml` not in PR diff; distribution job still `make dist` + `make verify-dist` |
| Blast radius | Packaging tool + tests only; `orgctl.py` untouched |

## Residual risk
Low. Temporary `update-ref` of `refs/heads/main` is process-local and restored; concurrent local use of `main` during `build` could race (same as any ref update) — acceptable for CI/release hosts running dist serially.

## Owner-complete
**NOT declared.**
