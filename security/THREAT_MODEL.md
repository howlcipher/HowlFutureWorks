# Agent Threat Model

Threats include prompt injection, tool misuse, privilege escalation, memory poisoning, data exfiltration, malicious inter-agent messages, approval bypass, recursive-agent abuse, poisoned dependencies, and runaway usage.

Security controls live in policy + execution enforcement, not Bot names.
