# Claude CLI entrypoint

Read `AGENTS.md` first. Treat it as the canonical CLI-agent instruction file.

For a specific Bot/role task, use `python tools/orgctl.py context <bot-id>` to print the minimum context file set before loading additional material.
