version: 1
rules:
  secrets_in_git: forbidden
  secrets_in_bot_prompt: forbidden
  prefer_scoped_service_accounts: true
  prefer_short_lived_credentials: true
  development_workers_receive_production_credentials: false
  revoke_when_no_longer_needed: true
