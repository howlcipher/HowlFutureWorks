# Skill: Reconcile Desired and Deployed Organization

Use `docs/RECONCILE_ORG.md`.

The repository is desired state. Live Grok configuration is deployed state. Compare normalized configuration, identify drift, auto-repair only within existing authority, and propose any authority-affecting change rather than silently applying it.
