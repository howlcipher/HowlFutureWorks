# Skill: Separate / Fire a Worker

Use `runbooks/fire-worker.md` for normal termination and `runbooks/suspend-worker.md` for immediate containment.

The worker record, tenure summary, curated context, contributions and handoffs must survive separation. Firing an employee is not the same as retiring the position.
