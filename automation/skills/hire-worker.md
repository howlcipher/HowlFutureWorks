# Skill: Propose / Complete a Hire

Use `runbooks/hire-worker.md`. A normal CLI agent may prepare the hire record and context packet, but live deployment and any new authority/credentials must follow current approval policy.

A new worker instance normally receives a new immutable employee ID even when filling an existing position.
