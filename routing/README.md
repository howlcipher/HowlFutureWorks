# Executor Routing

Do not encode permanent provider rankings in the constitution. `capability-registry.yaml` is the current evidence-based view; update it after representative evals.

Routing considers task class, risk, tools, quality, reliability, cost, latency, quota and context limits.
